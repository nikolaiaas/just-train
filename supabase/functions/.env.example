# Server-only secret for the local Edge Function runtime. Never add this to an
# Expo or NEXT_PUBLIC environment, and never commit the real value.
OPENROUTER_API_KEY=replace-with-a-limited-development-key
